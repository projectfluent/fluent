this.downloaded1 = function (env, sys) {
    return "\
  Pozostało \
  <span l10n-path='span[2]' title='" + (sys.getent(env, sys, "timeremaining1") / 60 + (" min'>\
    " + (sys.getent(env, sys, "timeremaining1") + (" sek\
  </span>.\
  Pobrałeś\
  <span l10n-path='span[1]' title='" + (sys.getent(env, sys, "kilobytes1") / 1024 + (" MB (megabajtów)'>\
    " + (sys.getent(env, sys, "kilobytes1") + " KB (kilobajtów)\
  </span>.\
")))))));
};
